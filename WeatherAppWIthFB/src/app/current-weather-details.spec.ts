import { CurrentWeatherDetails } from './current-weather-details';

describe('CurrentWeatherDetails', () => {
  it('should create an instance', () => {
    expect(new CurrentWeatherDetails()).toBeTruthy();
  });
});
